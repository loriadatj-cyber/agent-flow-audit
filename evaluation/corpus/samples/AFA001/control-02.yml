name: Read-only prompt flow 2
on: issue_comment
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Claude evaluation 2
        id: agent
        uses: anthropics/claude-code-action@v1
        with:
          prompt: "${{ github.event.comment.body }}"
