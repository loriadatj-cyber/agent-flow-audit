name: Read-only prompt flow 6
on: discussion
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Codex CLI evaluation 6
        id: agent
        run: |
          codex exec "${{ github.event.discussion.body }}"
