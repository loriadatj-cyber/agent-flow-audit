name: Privileged prompt flow 5
on: pull_request_review_comment
permissions:
  actions: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Repository agent evaluation 5
        id: agent
        uses: example/ai-agent@v1
        with:
          prompt: "${{ github.event.comment.body }}"
