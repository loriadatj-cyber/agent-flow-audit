name: Read-only prompt flow 7
on: discussion_comment
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Claude CLI evaluation 7
        id: agent
        run: |
          claude "${{ github.event.comment.body }}"
