name: Agent OIDC capability 7
on: workflow_dispatch
permissions:
  contents: read
  id-token: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Claude CLI evaluation 7
        id: agent
        run: |
          claude "Review the deployment manifest."
