name: Agent without OIDC 6
on: workflow_dispatch
permissions:
  contents: read
  id-token: none
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Codex CLI evaluation 6
        id: agent
        run: |
          codex exec "Review the deployment manifest."
