name: Agent without OIDC 4
on: workflow_dispatch
permissions:
  contents: read
  id-token: none
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Agentic Workflow evaluation 4
        id: agent
        uses: github/gh-aw@v1
        with:
          prompt: "Review the deployment manifest."
