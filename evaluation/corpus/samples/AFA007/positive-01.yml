name: Agent OIDC capability 1
on: workflow_dispatch
permissions:
  contents: read
  id-token: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Codex evaluation 1
        id: agent
        uses: openai/codex-action@v1
        with:
          prompt: "Review the deployment manifest."
