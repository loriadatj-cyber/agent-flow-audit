name: Broad agent capability 4
on: pull_request_review
permissions:
  discussions: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Agentic Workflow evaluation 4
        id: agent
        uses: github/gh-aw@v1
        with:
          prompt: "Review the checked-in policy file."
