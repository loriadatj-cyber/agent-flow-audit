name: Broad agent capability 7
on: discussion_comment
permissions:
  deployments: write
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Claude CLI evaluation 7
        id: agent
        run: |
          claude "Review the checked-in policy file."
