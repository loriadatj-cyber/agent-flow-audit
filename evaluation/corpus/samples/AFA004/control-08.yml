name: Read-only agent capability 8
on: pull_request_target
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Gemini CLI evaluation 8
        id: agent
        run: |
          gemini "Review the checked-in policy file."
