name: Quoted event environment 3
on: pull_request
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Print quoted environment
        run: "printf '%s\\n' \"$EVENT_TEXT\""
        env:
          EVENT_TEXT: ${{ github.event.pull_request.title }}
