name: Quoted event environment 4
on: pull_request_review
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Print quoted environment
        run: "printf '%s\\n' \"$EVENT_TEXT\""
        env:
          EVENT_TEXT: ${{ github.event.review.body }}
