name: Quoted event environment 1
on: issues
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Print quoted environment
        run: "printf '%s\\n' \"$EVENT_TEXT\""
        env:
          EVENT_TEXT: ${{ github.event.issue.body }}
