name: Event data shell flow 7
on: discussion_comment
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Print event data
        run: "printf '%s\\n' ${{ github.event.comment.body }}"
