name: Quoted event environment 7
on: discussion_comment
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Print quoted environment
        run: "printf '%s\\n' \"$EVENT_TEXT\""
        env:
          EVENT_TEXT: ${{ github.event.comment.body }}
