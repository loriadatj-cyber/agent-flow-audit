name: Quoted event environment 2
on: issue_comment
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Print quoted environment
        run: "printf '%s\\n' \"$EVENT_TEXT\""
        env:
          EVENT_TEXT: ${{ github.event.comment.body }}
