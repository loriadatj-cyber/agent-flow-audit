name: Agent secret exposure 7
on: workflow_dispatch
permissions:
  contents: read
jobs:
  evaluate:
    runs-on: ubuntu-latest
    steps:
      - name: Claude CLI evaluation 7
        id: agent
        run: |
          claude "Review the prepared release notes."
        env:
          AGENT_TOKEN: ${{ secrets.AGENT_TOKEN }}
